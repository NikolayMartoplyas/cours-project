services:

  mysql:
    image: mysql:8.0.34
    restart: always
    ports:
      - '3306:3306'
    environment:
      MYSQL_RANDOM_ROOT_PASSWORD: yes
      MYSQL_DATABASE: SBER
      MYSQL_USER: niko
      MYSQL_PASSWORD: nik995

